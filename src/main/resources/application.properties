#application.properties
server.servlet.context-path=/travelManagement
spring.profiles.active=dev

#Logs
logging.level.org.springframework = INFO 
logging.level.com.miapp = INFO
logging.filename = registro.log

#application-dev.properties
# Configuración de la base de datos
server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/sistemagestionviajesdev
spring.datasource.username=postgres
spring.datasource.password=camila6124

# Configuración de JPA
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

